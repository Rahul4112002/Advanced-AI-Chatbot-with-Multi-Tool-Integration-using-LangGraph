# Core LangGraph and LangChain dependencies
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.1.0
langchain-community>=0.3.0

# Streamlit for frontend
streamlit>=1.28.0

# Environment variables
python-dotenv>=1.0.0

# HTTP requests
requests>=2.31.0

# Database dependencies (SQLite is built-in but these may be needed for advanced features)
sqlalchemy>=2.0.0

# Additional dependencies that may be required by LangChain components
pydantic>=2.0.0
typing-extensions>=4.0.0

# Optional: For better performance and additional features
# tiktoken>=0.5.0  # For OpenAI token counting
# faiss-cpu>=1.7.0  # For vector storage (if needed)
# chromadb>=0.4.0  # Alternative vector database (if needed)